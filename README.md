
  # Separate Wireframe Frames (Copy)

  This is a code bundle for Separate Wireframe Frames (Copy). The original project is available at https://www.figma.com/design/n67Tp5djbmHdTb0HvtwzG3/Separate-Wireframe-Frames--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  